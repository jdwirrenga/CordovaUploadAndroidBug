import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CordovaUploadAndroidBug',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
